name: Release

on:
  pull_request:
    types:
      - closed
    branches:
      - main

jobs:
  develop:
    name: Test and lint
    uses: onlinex/recommendation-engine/.github/workflows/development.yaml@main

#  release:
#    runs-on: ubuntu-latest
#    name: Build & Release
#    if: github.event.pull_request.merged
#    steps:
#      -
#        name: ☁️ Checkout
#        uses: actions/checkout@v3
#        with:
#          ref: ${{ github.head_ref }}
#          fetch-depth: 0
#      -
#        run: echo "New Version ${{ steps.gitVersion.outputs.version }}."
#      - 
#        name: success
#        run: exit 0